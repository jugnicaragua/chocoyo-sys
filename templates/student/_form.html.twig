{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary"> <i class="fa fa-floppy-o" aria-hidden="true"></i> {{ button_label|default('Save') }}</button>
{{ form_end(form) }}
